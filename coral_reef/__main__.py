from coral_reef.ml.train import train

if __name__ == "__main__":
    # train(data_train, data_valid, image_base_dir, instructions)

    # image_base_dir = presumabily a directory with all base images (data/images?)
    print('heey')
